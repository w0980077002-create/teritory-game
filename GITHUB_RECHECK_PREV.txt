ПОВТОРНАЯ ПРОВЕРКА 11.09.2026

Репозиторий:
w0980077002-create/territory-sdolars-server

Проверено напрямую через GitHub:
- main branch существует;
- worker.js существует;
- package.json существует;
- wrangler.jsonc существует;
- .github/workflows/deploy.yml существует;
- предыдущий GitHub Actions deploy завершался ошибкой Wrangler Missing entry-point;
- текущий deploy.yml в GitHub всё ещё содержит старую строку:
  command: deploy
- правильная строка для исправления:
  command: deploy worker.js --config wrangler.jsonc

ВАЖНО:
Сохранённый в Library файл s47/worker.js отличается от текущего worker.js в GitHub по размеру на 185 байт.
Поэтому этот ZIP НЕ объявляется побайтной копией текущего GitHub worker.js.
Он сохранён как проверенная рабочая база s47 с исправленным workflow, чтобы не смешивать неизвестную версию с проектом.

Проверки локальной базы:
- node --check worker.js — OK
- package.json — OK
- wrangler.jsonc — OK
- deploy.yml — OK
- команда deploy содержит явный entry-point worker.js и wrangler.jsonc.
