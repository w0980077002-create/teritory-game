TERITORY GAME — AUTOMATIC REPOSITORY AUDIT / V6
Repository: w0980077002-create/teritory-game
Branch: main

RESULT
------
The CSS filenames referenced by the live index.html DO exist in the GitHub root.
The previous V5 failure was not caused by those four CSS filenames being absent.

Confirmed GitHub root CSS files:
  OK  style.css
  OK  game.css
  OK  game-runtime-fix.css
  OK  home-rebuild.css
  OK  arena.css
  OK  arena-mobile-layout.css
  OK  follower-arena.css
  OK  followers.css
  OK  forge-v2.css
  OK  hero.css
  OK  pve.css
  OK  reference-ui.css

Actual root approved Arena PNG files:
  OK  arena-night-approved.png
  OK  player-viking-approved.png
  OK  opponent-viking-approved.png

Important mismatch found:
1) home-rebuild.js referenced ./teritory_reference_bg.png
   but the real repository file is ./territory_reference_bg.png
   -> FIXED in V6.

2) arena.js and arena.css referenced ./arena-assets/... paths
   but the real GitHub repository has no arena-assets directory; the
   approved PNGs are physically in the repository root.
   -> FIXED in V6 to point to the real root approved PNG files.

3) index.html previously loaded only:
   style.css, home-rebuild.css, arena.css, reference-ui.css.
   The repository also contains required screen-specific CSS files.
   -> V6 index.html now loads all existing relevant CSS files from root,
      with one consistent cache-buster v=8020.

4) nginx.conf contained stale 8001/8002 sub_filter values while index.html
   used newer cache-busters.
   -> V6 synchronized nginx cache-busting to v=8020.

arena-assets requirement:
-------------------------
The requested arena-assets/ directory does NOT physically exist in main.
Therefore it is incorrect to pretend those files are inside that folder.
V6 uses the actual lowercase filenames exactly as they exist on GitHub:
arena-night-approved.png
player-viking-approved.png
opponent-viking-approved.png

No image files were renamed or invented.

V6 changed/replacement files:
------------------------------
index.html
home-rebuild.js
arena.js
nginx.conf
and the complete CSS/JS set already present in V5 so the package is
self-contained for the referenced local styles/scripts.

Missing local CSS after packaging:
arena-mobile-layout.css, follower-arena.css, followers.css, forge-v2.css, game-runtime-fix.css, game.css, hero.css, pve.css

Note:
The package does not contain duplicated binary PNGs because the approved
PNG files already exist in the GitHub root and the phone workflow should
not create duplicate artwork. Uploading the four corrected files above
keeps the original art intact.
