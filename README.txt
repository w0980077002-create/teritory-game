TERRITORY — Arena Clean Fix

This package contains a replacement arena.js.

Changes:
- Arena uses exactly 4 attack/defense zones: Head, Chest, Stomach, Legs.
- Each turn the player must select 1 attack zone and exactly 2 defense zones.
- Removed the old 5-zone Waist option.
- Removed skills/autobattle clutter from the Arena frontend.
- Keeps multiplayer modes explicitly marked as network-stage, not fake local multiplayer.
- Adds a defensive boot guard that resets stale Game/reward modals restored by Telegram WebView and returns the app to the City screen on load.

GitHub integration in this session is read-only (the attempted write returned HTTP 403), so this file could not be committed automatically.
Replace the repository's arena.js with this file.
