TERITORY GAME v9300 — IMPORTANT

The existing GitHub file named:
territory_reference_bg.png
contains JPEG image bytes despite the .png extension.

Before uploading the v9300 files, rename that existing GitHub file EXACTLY:
territory_reference_bg.png  ->  territory_reference_bg.jpg

Do NOT convert, edit, resize, or replace the image itself.
Only change the filename extension so nginx serves it as JPEG.

The new HOME code loads territory_reference_bg.jpg first and has a fallback to .png.
