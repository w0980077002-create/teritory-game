# Territory S66 — Guild Hard Redirect Fix

Исправление поверх S66.

- Кнопка «Гильдия» теперь напрямую вызывает новый S66-экран.
- Legacy `openSheet('Гильдия')` перенаправляется на `s66Guild()`.
- Старое окно гильдии больше не должно открываться через нижнее меню.
- JavaScript проверен через Node.js --check.
