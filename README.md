# Territory — Sdolars S90 CLEAN FON

Исправлена именно проблема отображения фона.

Что изменено:
- заменён реальный `assets/home-screen.png`, который подхватывает игра;
- удалена старая повреждённая/размазываемая сцена;
- новая сцена единая: Viking + замок + горы + водопады + порт;
- фон не растягивается и не собирается из нескольких кусков;
- добавлен `?v=s90` к asset для обхода старого browser/cache;
- `/` и `/index.html` отдаются Worker с `Cache-Control: no-store`;
- функциональные кнопки остаются поверх сцены;
- backend S88 FIXED сохранён.

После загрузки этой версии в Cloudflare/GitHub нужно открыть Mini App заново, чтобы Telegram получил новый `index.html`.
