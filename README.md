# Territory — City v2

Продолжение главного экрана Sdolars.

Изменение:
- `sdolars_clean_scene.png` используется как полноценный фон города.
- `scene_smallhero.jpg` остаётся только портретом в профиле.
- `app.js`, `arena.css`, `arena.js`, `game.css` не заменяются и не удаляются.

Установка:
1. Распаковать ZIP.
2. Заменить в GitHub только `index.html` и `style.css`.
3. Остальные файлы не трогать.
