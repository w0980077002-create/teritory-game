# Territory — Sdolars / s16

Визуальный экран приведён к компактному стилю:
- фон города резкий, без размытия;
- город используется только как environment;
- кнопки локаций маленькие и полупрозрачные;
- старые дублирующие панели удалены;
- «Казино» переименовано в `Game`;
- все кнопки остаются настоящими HTML/JS;
- фон встроен в `index.html`;
- Cloudflare / Worker / Wrangler / Three.js CDN отсутствуют.

Загружать только `index.html` в GitHub.
