10040 — REAL BATTLE ENTRY FIX

IMPORTANT:
- This patch restores the HOME runtime that was no longer loaded by the current index.
- The 4th bottom HOME button (БОЙ) directly starts the runner: hero moves/attacks bots.
- It does NOT open КВЕСТЫ and does NOT require manually opening the map.
- 240-chapter API is the source of truth when available.
- 4 bot wins = 25% each = 100%.
- At 100% the runner STOPS and returns HOME.
- The boss is NOT started automatically.
- The ☠️ skull on HOME is the boss entry after 100%.
- Arena is untouched.

Replace the files in this ZIP over the current repository.
