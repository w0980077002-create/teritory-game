/* Territory Game — STEP-04-E
   Arena with real active-follower abilities.
   Follower state is read only from TerritoryStore/Follower core.
*/
(function(){
'use strict';
const S=()=>window.TerritoryStore?.state||{};
const save=()=>window.TerritoryStore?.saveNow?.('arena');
const $=(s,r=document)=>r.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const zones=[['head','Голова'],['chest','Грудь'],['waist','Пояс'],['legs','Ноги']];
const styles={crit:{name:'Крит',mods:{attack:5,defense:0,crit:.14,dodge:.03,resilience:0,maxHp:0}},tank:{name:'Танк',mods:{attack:1,defense:8,crit:0,dodge:-.02,resilience:.12,maxHp:35}},dodge:{name:'Уворот',mods:{attack:2,defense:1,crit:.04,dodge:.14,resilience:0,maxHp:0}},resilience:{name:'Стойкость',mods:{attack:2,defense:4,crit:.02,dodge:0,resilience:.22,maxHp:18}}};
const gearSlots=[['weapon','⚔️','Оружие'],['helmet','🪖','Шлем'],['armor','🛡️','Броня'],['belt','🎗️','Пояс'],['boots','🥾','Сапоги'],['ring','💍','Кольцо'],['amulet','🔮','Амулет']];
const combatSlots=[['elixir_hp','🧪','HP'],['elixir_energy','🔵','Энергия'],['elixir_attack','🔥','Атака'],['elixir_guard','🛡️','Защита'],['adrenaline','⚡','Адреналин'],['speed_scroll','📜','Ускорение'],['anti_speed_scroll','🐌','Антиускорение']];
const followerRole={liabro:'crit',teralel:'tank',king_cows:'tank',mort:'dodge',stone_face:'resilience'};
let battle=null;
function root(){const m=$('#arenaModal'),b=$('#arenaModalBody');return m&&b?{m,b}:null}
function follower(){const s=S(),id=s.followers?.activeFollower,f=id&&window.Followers?.get?.(id),cfg=id&&window.Followers?.CATALOG?.[id],stats=id&&window.Followers?.getStats?.(id);return f?.owned&&cfg&&stats?{id,data:f,cfg,stats}:null}
function roster(){const l=Math.max(1,Number(S().level)||1),n=['Эйрик','Хальвдан','Сигурд','Рагнар','Ивар','Бьёрн'];return n.map((name,i)=>({id:'arena-bot-'+i,name,level:Math.max(1,l+i%3-1),class:['duelist','berserker','tank','assassin'][i%4],rating:820+(5-i)*42,wins:18+i*7,losses:6+i*3,isBot:true}))}
function me(){const s=S();return{id:'player',name:String(s.profile?.displayName||s.name||'Игрок'),level:Math.max(1,Number(s.level)||1),rating:Number(s.arena?.rating)||1000,wins:Number(s.arena?.wins)||0,losses:Number(s.arena?.losses)||0,class:'duelist'}}
function profile(id){return id==='player'?me():roster().find(x=>x.id===id)}
function loadout(){const k=String(S().arena?.loadout||'crit');return styles[k]?k:'crit'}
function open(){openHub()}
function openHub(){stopTimer();const r=root();if(!r)return;r.m.classList.remove('arena-in-battle');battle=null;r.b.innerHTML=`<div class="arena-hub"><div class="arena-hero"><div><small>СОРЕВНОВАТЕЛЬНАЯ АРЕНА</small><h1>⚔️ АРЕНА</h1><p>Тактические бои 1×1.</p></div><div class="arena-rating"><span>РЕЙТИНГ</span><b>${me().rating}</b><small>${me().wins} побед · ${me().losses} поражений</small></div></div><button class="arena-start" data-arena-find>⚔️ НАЙТИ СОПЕРНИКА</button><div class="arena-section-title"><b>🏆 ТОП АРЕНЫ</b></div><div class="arena-top">${roster().map((p,i)=>`<button class="arena-top-row" data-profile-id="${p.id}"><strong>${i+1}</strong><span class="arena-avatar">⚔️</span><span class="arena-player-copy"><b>${esc(p.name)}</b><small>Lv.${p.level}</small></span><span class="arena-player-rating">${p.rating}</span></button>`).join('')}</div></div>`;r.m.classList.add('show');r.m.setAttribute('aria-hidden','false')}
function openProfile(id){const p=profile(id),r=root();if(!p||!r)return;r.b.innerHTML=`<div class="arena-profile"><button class="arena-back" data-arena-hub>‹ АРЕНА</button><div class="profile-hero"><div class="profile-big-avatar">⚔️</div><div><small>ИГРОК</small><h1>${esc(p.name)}</h1><p>Уровень ${p.level}</p></div></div><div class="profile-stats"><div><small>РЕЙТИНГ</small><b>${p.rating}</b></div><div><small>ПОБЕДЫ</small><b>${p.wins}</b></div><div><small>ПОРАЖЕНИЯ</small><b>${p.losses}</b></div><div><small>КЛАСС</small><b>${esc(p.class)}</b></div></div>${id!=='player'?`<button class="arena-start" data-profile-fight="${id}">⚔️ ВЫЗВАТЬ НА БОЙ</button>`:''}</div>`}
function fighter(p,side){const s=S(),f=side==='player'?follower():null,k=side==='player'?(followerRole[f?.id]||loadout()):(p.class==='tank'?'tank':p.class==='assassin'?'dodge':p.class==='berserker'?'crit':'resilience'),m=styles[k].mods;const fb=side==='player'&&f?Math.floor((Number(f.stats.attack)||0)/4):0,fd=side==='player'&&f?Math.floor((Number(f.stats.defense)||0)/4):0,fh=side==='player'&&f?Math.floor((Number(f.stats.hp)||0)/20):0;const max=side==='player'?Math.max(1,Number(s.maxHp)||120)+m.maxHp+fh:100+p.level*12+m.maxHp;return{id:side,name:p.name,level:p.level,hp:max,maxHp:max,attack:(Number(s.strength)||8)+(m.attack||0)+fb,defense:(Number(s.defense)||3)+(m.defense||0)+fd,crit:Math.min(.75,.06+(m.crit||0)+(f?.id==='liabro'?Number(f.stats.critChance||0)/100:0)),dodge:Math.min(.75,Math.max(0,.03+(m.dodge||0)+(f?.id==='mort'?Number(f.stats.dodge||0)/100:0))),resilience:m.resilience||0,style:k,follower:f?{id:f.id,name:f.cfg.name,role:f.cfg.role,level:f.data.level,stats:f.stats}:null}}
function start(op,mode='1v1'){const r=root();if(!r)return;const f=follower();battle={mode,player:fighter(me(),'player'),bot:fighter(op,'bot'),opponent:op,follower:f,turn:1,playerDefense:[],attackZone:'',botDefense:[],auto:false,busy:false,logs:[`⚔️ Бой начался: ${me().name} против ${op.name}`],chat:[],ended:false,pendingResult:null,startedAt:Date.now(),playerReadyAt:0,botReadyAt:0,baseCooldown:60000,timer:null,botStunned:false};if(f)battle.logs.push(`✦ ${f.cfg.name} вступил в бой: ${f.cfg.role}`);render();startTimer()}
function followerBadge(){const f=battle?.follower;if(!f)return'';return `<div class="active-follower-badge"><span>${f.cfg.icon||'✦'}</span><b>${esc(f.cfg.name)}</b><small>${esc(f.cfg.role)} · ур.${f.data.level}</small></div>`}
function render(){const r=root(),p=battle.player,b=battle.bot;if(!r)return;r.m.classList.add('arena-in-battle');r.b.innerHTML=`<div class="arena-battle" data-battle-root><div class="battle-header"><button class="arena-back" data-arena-hub>‹</button><b>ХОД ${battle.turn}</b><span>Рейтинг ${me().rating}</span>${battle.follower?`<span class="active-follower-header">${battle.follower.cfg.icon||'✦'} ${esc(battle.follower.cfg.name)} · ур.${battle.follower.data.level}</span>`:''}</div><div class="battle-stage"><div class="fighter-wrap player-wrap">${fighterMarkup(p,'player')}${followerBadge()}</div><div class="fighter-wrap bot-wrap">${fighterMarkup(b,'bot')}</div><div class="battle-zone-controls defense-controls"><div class="side-zone-title">ЗАЩИТА · 2</div>${zones.map(z=>`<button data-defense-zone="${z[0]}">${z[1]}</button>`).join('')}</div><div class="battle-zone-controls attack-controls"><div class="side-zone-title">АТАКА · 1</div>${zones.map(z=>`<button data-attack-zone="${z[0]}">${z[1]}</button>`).join('')}</div><div class="combat-effects" data-effects></div></div><div class="battle-command-row"><span class="tactic-mini">2 🛡️ + 1 ⚔️</span><button class="command-auto" data-autobattle-toggle aria-pressed="${battle.auto}"><span>↻</span><small>${battle.auto?'АВТО ✓':'АВТО'}</small></button><button class="command-hit" data-execute-attack disabled>⚔️ УДАР</button><button class="command-surrender" data-surrender>Сдаться</button><button class="command-exit" data-exit-battle>Выйти</button><strong data-cooldown>Готов</strong></div>${combatBar()}<div class="battle-chat-wrap"><button class="battle-chat-toggle" data-chat-toggle aria-expanded="false">💬 История и чат <span>⌄</span></button><div class="battle-chat collapsed" data-chat>${chatHtml()}</div></div>${bottomNav()}</div>`;sync();bindBattleControls()}
function fighterMarkup(u,side){return `<div class="combat-fighter ${side}" data-fighter="${side}"><div class="fighter-name">${esc(u.name)} <small>Lv.${u.level}</small></div><div class="fighter-hp-top"><span class="hp-fill" style="width:${Math.max(0,Math.round(u.hp/u.maxHp*100))}%"></span><b>${u.hp}/${u.maxHp} ❤️</b></div><div class="fighter-shadow"></div></div>`}
function combatBar(){const unlocked=Math.max(0,Math.min(6,Number(S().arena?.combatSlotsUnlocked??3)));return `<div class="combat-loadout-strip"><div class="combat-section-head"><b>СНАРЯЖЕНИЕ</b></div><div class="combat-item-row gear-row">${gearSlots.map(g=>`<button class="combat-item-slot" data-gear="${g[0]}"><strong>${g[1]}</strong><span>${g[2]}</span></button>`).join('')}</div><div class="combat-section-head consumables-head"><b>ЭЛИКСИРЫ И БОЕВЫЕ ПРЕДМЕТЫ</b></div><div class="combat-item-row elixir-row">${combatSlots.map((g,i)=>{const locked=i>unlocked;return `<button class="combat-item-slot ${locked?'locked':''}" data-combat-slot="${g[0]}"><strong>${locked?'🔒':g[1]}</strong><span>${g[2]}</span><small>${locked?'Открывается':'×'+Number(S().consumables?.[g[0]]||0)}</small></button>`}).join('')}</div></div>`}
function chatHtml(){return [...battle.logs.map(x=>`<div class="chat-line system">${esc(x)}</div>`),...battle.chat.map(x=>`<div class="chat-line"><b>${esc(x.name)}:</b> ${esc(x.text)}</div>`)].join('')+`<div class="chat-compose"><input data-chat-input maxlength="180" placeholder="Написать сообщение…"><button data-chat-send>➤</button></div>`}
function bottomNav(){return `<nav class="arena-bottom-nav">${[['home','🏰','Город'],['inventory','🎒','Инвентарь'],['hero','🪖','Герой'],['battle','⚔️','Бой'],['quests','📜','Квесты'],['game','🎲','Игры'],['clan','🚩','Клан']].map(x=>`<button data-arena-nav="${x[0]}"><span>${x[1]}</span><b>${x[2]}</b></button>`).join('')}</nav>`}
function startTimer(){stopTimer();if(!battle)return;battle.timer=setInterval(()=>{if(!battle)return;const now=Date.now();if(battle.pendingResult&&now>=battle.pendingResult.readyAt){const r=battle.pendingResult;battle.pendingResult=null;finalize(r.win);return}if(!battle.ended&&!battle.pendingResult&&battle.botReadyAt&&now>=battle.botReadyAt){battle.botReadyAt=0;botTurn();return}if(!battle.ended&&!battle.pendingResult&&battle.auto&&cooldownLeft()<=0)autoStep();sync()},250)}
function stopTimer(){if(battle?.timer){clearInterval(battle.timer);battle.timer=null}}
function cooldownLeft(){return Math.max(0,(battle?.playerReadyAt||0)-Date.now())}
function botCooldownLeft(){return Math.max(0,(battle?.botReadyAt||0)-Date.now())}
function botDelay(){return 15000+Math.floor(Math.random()*36000)}
function minBattleLeft(){return battle&&battle.mode!=='1v1'?Math.max(0,30000-(Date.now()-battle.startedAt)):0}
function formatClock(ms){const s=Math.max(0,Math.ceil(ms/1000));return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}
function bindBattleControls(){const r=root();if(!r||!battle)return;const exec=r.b.querySelector('[data-execute-attack]');if(exec)exec.onclick=executeAttack;const auto=r.b.querySelector('[data-autobattle-toggle]');if(auto)auto.onclick=toggleAuto;r.b.querySelectorAll('[data-defense-zone]').forEach(x=>x.onclick=()=>selectDefense(x.dataset.defenseZone));r.b.querySelectorAll('[data-attack-zone]').forEach(x=>x.onclick=()=>selectAttack(x.dataset.attackZone));r.b.querySelectorAll('[data-combat-slot]').forEach(x=>x.onclick=()=>useSlot(x.dataset.combatSlot));const toggle=r.b.querySelector('[data-chat-toggle]');if(toggle)toggle.onclick=()=>{const box=r.b.querySelector('[data-chat]');const open=box.classList.toggle('collapsed')===false;toggle.setAttribute('aria-expanded',open);toggle.querySelector('span').textContent=open?'⌃':'⌄'}}
function selectDefense(z){if(!battle||battle.busy||battle.ended||battle.pendingResult||cooldownLeft()>0)return;const i=battle.playerDefense.indexOf(z);if(i>=0)battle.playerDefense.splice(i,1);else if(battle.playerDefense.length<2)battle.playerDefense.push(z);sync()}
function selectAttack(z){if(!battle||battle.busy||battle.ended||battle.pendingResult||cooldownLeft()>0)return;battle.attackZone=z;sync()}
function toggleAuto(){if(!battle||battle.ended)return;battle.auto=!battle.auto;if(battle.auto&&cooldownLeft()<=0)autoStep();sync()}
function sync(){const r=root();if(!r||!battle)return;const cd=cooldownLeft(),min=minBattleLeft();r.b.querySelectorAll('[data-defense-zone]').forEach(x=>{x.classList.toggle('selected',battle.playerDefense.includes(x.dataset.defenseZone));x.disabled=battle.busy||battle.ended||!!battle.pendingResult||cd>0});r.b.querySelectorAll('[data-attack-zone]').forEach(x=>{x.classList.toggle('selected',x.dataset.attackZone===battle.attackZone);x.disabled=battle.busy||battle.ended||!!battle.pendingResult||cd>0});const btn=r.b.querySelector('[data-execute-attack]'),clock=r.b.querySelector('[data-cooldown]');const ready=battle.playerDefense.length===2&&!!battle.attackZone&&!battle.busy&&!battle.ended&&!battle.pendingResult&&cd<=0;if(btn)btn.disabled=!ready;if(clock)clock.textContent=cd>0?`Удар ${formatClock(cd)}`:battle.pendingResult?`Минимум ${formatClock(min)}`:botCooldownLeft()>0?`Бот ${formatClock(botCooldownLeft())}`:'Готов к ходу';r.b.querySelector('[data-autobattle-toggle]')?.classList.toggle('active',battle.auto)}
function effect(type,side){const layer=$('[data-effects]');if(!layer)return;const e=document.createElement('div');e.className=`combat-fx fx-${type} fx-${side}`;e.textContent=type==='crit'?'✦':type==='block'?'✧':type==='heal'?'♥':type==='control'?'☠':type==='hit'?'✹':'×';layer.appendChild(e);setTimeout(()=>e.remove(),700)}
function animateFighter(side,cls){const el=$(`[data-fighter="${side}"]`);if(!el)return;el.classList.remove(cls);void el.offsetWidth;el.classList.add(cls);setTimeout(()=>el.classList.remove(cls),800)}
function executeAttack(){if(!battle||battle.busy||battle.ended||battle.pendingResult||cooldownLeft()>0||battle.playerDefense.length!==2||!battle.attackZone)return;battle.busy=true;const p=battle.player,b=battle.bot,f=battle.follower;animateFighter('player','attack');let dmg=Math.max(1,(p.attack+(battle.attackBuff||0))-b.defense);battle.attackBuff=0;if(Math.random()<b.dodge){battle.logs.push('💨 Соперник увернулся');effect('miss','bot');animateFighter('bot','dodge')}else{const blocked=battle.botDefense.includes(battle.attackZone);if(blocked){dmg=Math.max(1,Math.floor(dmg*.25));battle.logs.push(`🛡️ Соперник заблокировал удар: ${dmg}`);effect('block','bot');animateFighter('bot','block')}else if(Math.random()<p.crit){dmg=Math.floor(dmg*1.8);battle.logs.push(`💥 Критический удар: ${dmg}`);effect('crit','bot');animateFighter('bot','hurt')}else{battle.logs.push(`⚔️ Ты нанёс ${dmg} урона`);effect('hit','bot');animateFighter('bot','hurt')}b.hp=Math.max(0,b.hp-dmg);if(f?.id==='stone_face'&&b.hp>0&&Math.random()<Math.min(.35,(Number(f.stats.control)||0)/100)){battle.botStunned=true;battle.logs.push(`💀 ${f.cfg.name}: контроль! Следующий ход соперника пропущен.`);effect('control','bot')}}battle.playerReadyAt=Date.now()+battle.baseCooldown;battle.botReadyAt=Date.now()+botDelay();battle.busy=false;battle.attackZone='';if(b.hp<=0){finish(true);return}render()}
function botTurn(){if(!battle||battle.ended||battle.pendingResult)return;if(battle.botStunned){battle.botStunned=false;battle.logs.push(`💀 ${battle.follower?.cfg?.name||'Спутник'} остановил атаку соперника.`);battle.turn++;battle.playerDefense=[];battle.attackZone='';render();return}const p=battle.player,b=battle.bot,f=battle.follower,atk=zones[Math.floor(Math.random()*4)][0],d1=zones[Math.floor(Math.random()*4)][0];let d2=zones[Math.floor(Math.random()*4)][0];while(d2===d1)d2=zones[Math.floor(Math.random()*4)][0];battle.botDefense=[d1,d2];let dmg=Math.max(1,b.attack-(p.defense+(battle.guardBuff||0)));battle.guardBuff=0;if(f?.id==='teralel')dmg=Math.max(1,Math.floor(dmg*Math.max(.70,1-Math.min(.30,Number(f.stats.defense||0)/250))));animateFighter('bot','attack');if(p.dodge&&Math.random()<p.dodge){battle.logs.push('💨 Ты увернулся от удара');effect('miss','player')}else if(Math.random()<b.crit){dmg=Math.floor(dmg*1.8);p.hp=Math.max(0,p.hp-dmg);battle.logs.push(`💥 Критический удар соперника: ${dmg}`);effect('crit','player');animateFighter('player','hurt')}else{if(battle.playerDefense.includes(atk)){dmg=Math.max(1,Math.floor(dmg*.25));battle.logs.push(`🛡️ Ты заблокировал удар: ${dmg}`);effect('block','player');animateFighter('player','block')}else{p.hp=Math.max(0,p.hp-dmg);battle.logs.push(`⚔️ Соперник нанёс ${dmg} урона`);effect('hit','player');animateFighter('player','hurt')}}if(f?.id==='king_cows'&&p.hp>0&&p.hp<p.maxHp){const heal=Math.max(1,Math.floor(Math.min(40,Number(f.stats.heal||0)*.5)));p.hp=Math.min(p.maxHp,p.hp+heal);battle.logs.push(`❤️ ${f.cfg.name}: +${heal} HP`);effect('heal','player')}if(p.hp<=0){finish(false);return}battle.turn++;battle.playerDefense=[];battle.attackZone='';render()}
function autoStep(){if(!battle||battle.ended||battle.busy||battle.pendingResult||cooldownLeft()>0||!battle.auto)return;let d1=zones[Math.floor(Math.random()*4)][0],d2=zones[Math.floor(Math.random()*4)][0];while(d2===d1)d2=zones[Math.floor(Math.random()*4)][0];battle.playerDefense=[d1,d2];battle.attackZone=zones[Math.floor(Math.random()*4)][0];sync();setTimeout(()=>{if(battle?.auto&&!battle.ended&&cooldownLeft()<=0)executeAttack()},220)}
function finalize(win){if(!battle)return;battle.ended=true;battle.busy=false;battle.playerReadyAt=0;battle.botReadyAt=0;stopTimer();battle.logs.push(win?'🏆 Победа!':'☠️ Ты проиграл. Бой завершён.');render()}
function finish(win){if(!battle||battle.ended||battle.pendingResult)return;const wait=minBattleLeft();if(wait>0){battle.pendingResult={win,readyAt:Date.now()+wait};battle.busy=true;battle.logs.push('⏳ Исход определён. Минимальная длительность командного боя — 30 секунд.');sync();return}finalize(win)}
function sendChat(){if(!battle)return;const i=$('[data-chat-input]');if(!i||!i.value.trim())return;battle.chat.push({name:me().name,text:i.value.trim()});i.value='';render()}
function exitBattle(){if(!battle)return;stopTimer();openHub()}
function useSlot(slot){if(!battle||battle.ended||battle.busy)return;const idx=combatSlots.findIndex(x=>x[0]===slot),unlocked=Math.max(0,Math.min(6,Number(S().arena?.combatSlotsUnlocked??3)));if(idx<0||idx>unlocked)return;const c=S().consumables||{};if(Number(c[slot]||0)<=0)return;if(slot==='speed_scroll'&&cooldownLeft()>0){const floor=battle.mode!=='1v1'?battle.startedAt+30000:Date.now();battle.playerReadyAt=Math.max(floor,battle.playerReadyAt-10000);c[slot]--;save();battle.logs.push('📜 Ускорение: −10 секунд.');render();return}if(slot==='anti_speed_scroll'&&cooldownLeft()>0){battle.playerReadyAt=Math.min(Date.now()+60000,battle.playerReadyAt+10000);c[slot]--;save();battle.logs.push('🐌 Антиускорение: +10 секунд.');render();return}if(slot==='elixir_hp'){c[slot]--;battle.player.hp=Math.min(battle.player.maxHp,battle.player.hp+30);battle.logs.push('🧪 HP восстановлено на 30');save();render();return}if(slot==='elixir_energy'){c[slot]--;if(typeof S().energy==='number')S().energy=Math.min(100,S().energy+20);battle.logs.push('🔵 Энергия восстановлена на 20');save();render();return}if(slot==='elixir_attack'){c[slot]--;battle.attackBuff=(battle.attackBuff||0)+5;battle.logs.push('🔥 Атака усилена на следующий удар (+5)');save();render();return}if(slot==='elixir_guard'){c[slot]--;battle.guardBuff=(battle.guardBuff||0)+5;battle.logs.push('🛡️ Защита усилена на следующий удар (+5)');save();render();return}}
function chooseGear(slot){if(!battle||battle.ended)return;const keys=Object.keys(styles),k=keys[(keys.indexOf(battle.player.style)+1)%keys.length];battle.player.style=k;battle.logs.push(`👕 ${gearSlots.find(x=>x[0]===slot)?.[2]||'Предмет'}: комплект «${styles[k].name}»`);render()}
document.addEventListener('click',e=>{const t=e.target.closest?.('button');if(!t)return;if(t.id==='arenaClose'){root()?.m.classList.remove('show');return}if(t.dataset.arenaFind){start(roster()[Math.floor(Math.random()*roster().length)]);return}if(t.dataset.profileId){openProfile(t.dataset.profileId);return}if(t.dataset.arenaHub){openHub();return}if(t.dataset.profileFight){start(profile(t.dataset.profileFight));return}if(t.dataset.gear){chooseGear(t.dataset.gear);return}if(t.dataset.chatSend){sendChat();return}if(t.dataset.surrender){if(battle&&!battle.ended)finish(false);return}if(t.dataset.exitBattle){exitBattle();return}if(t.dataset.arenaNav){if(t.dataset.arenaNav==='battle')return;root()?.m.classList.remove('show');window.showScreen?.(t.dataset.arenaNav==='home'?'home':t.dataset.arenaNav);return}});
document.addEventListener('keydown',e=>{if(e.key==='Enter'&&document.activeElement?.matches('[data-chat-input]'))sendChat()});
window.ArenaGame={open,openHub,startBattle:start,close:()=>root()?.m.classList.remove('show')};
})();
